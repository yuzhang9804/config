{
  "name": "config",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "sync2Gh": "chmod +x sync2Git.sh && ./sync2Git.sh"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
